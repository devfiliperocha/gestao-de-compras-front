import styled from 'styled-components'
import { List as ListBase } from '@material-ui/core'

export const Wrapper = styled(ListBase)``

export const List = styled.div`
  height: 60vh;
  overflow-y: auto;
`
